# REPORT_TEST.md — 专题报告（只追加，按专题分节，不新建文件）

## [2026-08-13] 专题：负荷辨识算法开发流程技术方案与流水线骨架
- 类型：用户专题
- 目标与假设：在「母线 288 点/天（三相 U/I/P/PF）+ 分路 96 点/天（三相总有功）」数据条件下，产出模块化、解耦、支持多模型对比的负荷辨识开发流水线。假设：分路标签粒度决定辨识上限为分路级有功分解；训练主分辨率取 15min 可行且信息无损
- 方法 / 数据 / 参数：
  - 架构：common（契约/注册表/日志）→ data_io / preprocess / events / models / evaluation / reporting → pipeline 编排，单向依赖、接口隔离、配置驱动、注册表装配
  - 对齐：母线 5min→15min 均值聚合（PF 用 P/(U·I) 重算），时间重叠率门禁；防泄漏时序划分 70/15/15，滑窗不跨 split
  - 模型（已实现 3 个）：history_profile（槽位均值画像）、proportional（占比分摊）、ridge（numpy 闭式解）；GBDT/Seq2Point/LSTM 列入 M2
  - 约束后处理：非负 + 总和一致性投影（模型无关）
  - 验证：15 项单测（含端到端 smoke）+ 7 天合成数据 CLI 全流程演示
- 结果 / 结论：
  - 流水线端到端跑通：train→evaluate→compare 一键完成，产物（配置快照/模型/metrics/对比表/报告）自动归档 outputs/<exp>/<ts>/
  - 合成数据演示：三模型 R²≥0.996，proportional 综合最优（符合合成设定为固定占比的预期，验证对比机制正确性）
  - 新增模型成本 = 实现 BaseModel 接口 + 注册 + 配置加一行，编排/评估代码零改动（解耦达标）
- 是否进入 REPORT.md（稳定结论）：否（尚无真实数据实验结论）
- 遗留问题：真实数据接入与时间同步验证（M0）；GBDT/深度模型实现（M2）；超参扫描与选型报告（M3）；事件检测扩展（M3+）

## [2026-08-13] 专题：按《工商业负荷辨识算法开发指南 V2.1》代码重构
- 类型：用户专题
- 目标与假设：以指南为最高优先级接口契约重构 M1 骨架；假设：指南 §0–§13 条款可逐条映射为模块职责；单用户与多用户可统一为同一批量代码路径
- 方法 / 数据 / 参数：
  - 契约层集中管理（common/contracts.py）：RE_BUS/RE_BR/RE_USER_DIR 原文照抄、§13 状态码、§12.3 字段规则；其余模块仅引用常量，不改写正则
  - 数据接入：discovery 扫描（状态码判定）→ CsvBusLoader 多 Ch 关联 + bus_field_map 字段映射（CT/PT 倍率配置化）→ schema/质量报告（data_schema_report.json + data_quality_report.html）
  - 预处理：5min→15min 聚合策略可配置且落盘（agg_strategy.json）；τ 时滞只报告证据；§8 特征集（FFT/THD 禁用）；L=96 窗口默认 Seq2Seq；四种切分策略 + splits include/exclude 锚定（train→val→test 优先）；Scaler 仅 Train 拟合
  - 新增 analysis（§9 可辨识性，训练前强制）与 postprocess（on_thr_w/post_min_on/post_fill_short_off）模块
  - 编排：user_config（§12 键优先级/校验/user_id 显式映射层）→ user_task（单用户端到端 + 状态码异常）→ batch（失败隔离/_DONE 续跑/状态表 user_id 字段）
  - 验证：65 项测试（含 AST 解耦守卫、契约正则、时间过滤语义、复合目标 skipna=False、配置校验、批量状态码/隔离/续跑）；CLI 实测 2 合法用户 + 2 异常目录全流程
- 结果 / 结论：
  - 指南 §3–§13 全部可落地条款已实现并在测试中固化；依赖方向静态审计通过（业务模块仅依赖 common）
  - 多用户批量：OK 4 / DATA_MISSING_BUS 1 / INVALID_USER_DIR 1，失败隔离生效；续跑 SKIPPED_RESUME 生效；单用户模式同路径生效
  - 推理输出符合 predictions/inference_result.csv 契约（timestamp/user_id/target/pred/pred_state）
  - 识别 3 项接口待确认项（日级指标字段清单 25vs23、启动段字段契约、target_col 回退链），按指南 §0 口径记录不擅自补充
- 是否进入 REPORT.md（稳定结论）：否（工程重构专题，尚无真实数据实验结论）
- 遗留问题：指南附件缺失（字段清单/启动段契约）；真实数据 M0；M2 多模型；天气特征数据源未接入

## [2026-08-13] 专题：5 用户真实数据批量执行验证
- 类型：验证专题
- 目标与假设：用真实工商业数据（5 个 user_key，trains+infers）按指南 §12 入口批量执行，验证 V2.1 重构后的流水线在数据稀疏、哨兵值、量纲未确认等真实条件下可用、可诊断、可续跑
- 方法 / 数据 / 参数：
  - 数据：5 用户（842/844 稀疏 88/57 点/天，778/789/800 密集 282/288 点/天）；分路 4 列 p1..p4（842/844）或 2 列（其余）；配置 configs/time_filters.json（含复合目标 p1+p2、splits 锚定、train/infer include/exclude）
  - 入口：python scripts/run_batch_users.py --time-filter-config configs/time_filters.json（指南原文入口）
  - 模型：history_profile / proportional / ridge；指标 mae/rmse/r2/sae
  - 摸底手段：全列统计 + 哨兵值识别 + 与分路和相关性排序辨识字段语义
- 结果 / 结论：
  - 批量 10/10 OK（train 5 + infer 5）；best 分布：ridge×2、proportional×2、history_profile×1
  - 密集用户效果可用：842 ridge r2=0.629 sae=0.008；778 history_profile r2=0.826；800 ridge r2=0.716
  - 稀疏用户 844（bus 57 点/天、分路 16 点/天）与复合目标用户 789 基线 r2<0 → M2 需 GBDT/序列模型（结论入下一步）
  - 断点续跑：复跑 10/10 SKIPPED_RESUME；单用户模式同路径生效
  - 可辨识性：5 用户 pearson 0.74–0.90 全部 identifiable=True
  - 发现并修复 3 个真实数据暴露的问题：双哨兵值、对称重叠率口径对稀疏总线失真、低方差绝对阈值受未确认量纲污染（均固化单测）
- 是否进入 REPORT.md（稳定结论）：否（字段映射为临时映射，待点位表确认后再沉淀稳定结论）
- 遗留问题：点位表/倍率待确认；稀疏用户建模改善（M2）；指南附件字段契约待补充

## [2026-08-13] 专题：多数据源用户数据批量合并脚本
- 类型：用户专题
- 目标与假设：按需求文档实现「单源内部合并 → 跨源合并」两级脚本；假设：RE_BUS 文件名可承载合并所需全部维度（终端号/用户号/通道号/起止时间），文件名时间区间可作重叠判定依据（§4.1 原文：解析文件名中的起止时间区间）
- 方法 / 数据 / 参数：
  - nilm/data_io/merge.py：discover（RE_USER_DIR 目录 + RE_BUS 文件 + 身份一致性）→ 阶段一逐用户逐通道迭代两两合并（无重叠合并取最早/最晚，重叠抛 OverlapError 整组跳过）→ 阶段二跨源同键组合并到 cross_source/
  - 内容合并：时间列自动识别（event_time/time/timestamp/首列）、拼接去重（保留先出现）、排序、保留原列名
  - CLI：scripts/merge_user_data.py --sources 多源（必填）--output-root/--log-dir/--no-keep-original（可选）
  - 验证：6 项专项测试（含真实文件只读断言）；真实数据 data/trains+data/infers 两源实测
- 结果 / 结论：
  - 71 项测试全过（解耦守卫确认 merge 只依赖 common）
  - 真实数据实测：内源 10 组 OK（单文件保留原名）；跨源 5 组全部检出时间区间完全重合 → SKIPPED_OVERLAP，告警日志含四要素（源路径/用户目录/文件名/冲突区间），重叠保护机制在真实数据上有效
  - 输出层级复刻输入（数据源名/用户目录），满足溯源要求
- 是否进入 REPORT.md（稳定结论）：否（工程工具专题，无算法结论）
- 遗留问题：文件名时间区间与 CSV 内实际时间戳的一致性校验（指南 §3.2 要求以 CSV 时间戳为最终依据）可在 M0+ 增强；当前按需求文档以文件名为准

## [2026-08-13] 专题修订：合并脚本严格文件名格式（对上述合并专题的补充）
- 类型：用户专题（修订）
- 目标与假设：待合并文件名必须严格为 e241_<终端号>_<用户号>-Ch<通道号>-<起>-<止>.csv，无后缀；-1/-infer 后缀文件判定为不符合格式
- 方法 / 数据 / 参数：
  - 新增独立契约 RE_MERGE_FILE（无后缀严格式）+ parse_merge_filename；指南 RE_BUS 保持不变（两契约并存，互不放宽）
  - discover_source 仅接受严格格式文件；带后缀文件输出告警（注明需求文档 §2.2）并跳过
  - 新增测试：契约对照（严格式拒绝 -1/-infer，RE_BUS 仍接受）、后缀文件不混入合并的端到端验证
- 结果 / 结论：
  - 73 项测试全过；真实数据实测：10 个带 -1 后缀总线文件全部被拒绝并告警，无合并产物（行为符合用户规定）
  - 影响提示：如需合并现有真实数据，源文件须按严格格式命名（不改原始数据，由导出侧或副本重命名解决）
- 是否进入 REPORT.md（稳定结论）：否（工程契约修订）
- 遗留问题：真实数据文件命名何时切换为严格格式，取决于上游导出约定

## [2026-08-13] 专题修订②：单源独有用户透传（对合并专题的补充）
- 类型：用户专题（修订）
- 目标与假设：两源合并中，用户目录仅存在于其一（另一源不存在该用户目录）时，其待合并文件应直接成为合并后用户数据文件；假设：阶段一输出/原始文件可作为透传载体，通道粒度实现等价于用户目录粒度
- 方法 / 数据 / 参数：
  - 阶段二单源组：merge_group(keep_single=True) 复制至 cross_source/<user>/（原名原内容）；报告 action=copied_single_source、sources=[唯一源名]
  - 阶段一在 keep_original=False 时仍将单文件组（原始路径）登记给阶段二，保证透传不受单源保留选项影响
  - 新增测试：透传端到端（单源用户原样进入合并后目录 + 双源用户仍走跨源合并）、--no-keep-original 组合
- 结果 / 结论：
  - 75 项测试全过；真实数据回归：严格格式行为不变（10 个带后缀文件全部拒绝）
  - 合并后用户数据目录（cross_source/）自此为完整统一输出：跨源合并结果 + 单源透传结果
- 是否进入 REPORT.md（稳定结论）：否（工程行为修订）
- 遗留问题：无新增

## [2026-08-14] 专题修订③：分路格式文件合并支持（对合并专题的补充）
- 类型：用户专题（修订）
- 目标与假设：待合并文件新增 <用户号>-<起>-<止>.csv 严格格式（YYmmdd、无后缀），合并规则与总线完全一致；假设：分路文件无通道维度，按用户分组与总线「用户+通道」分组可并存
- 方法 / 数据 / 参数：
  - RE_MERGE_BRANCH 严格契约（拒绝 -1/-infer 后缀）；BusFile 增加 kind 维度，group_key 分别为 (user_key,"branch") 与 (user_key,ch)
  - 两级合并、重叠告警跳过整组、单源透传、结构复刻等规则零改动复用
  - 新增 4 项测试覆盖契约与端到端路径
- 结果 / 结论：
  - 79 项测试全过；真实数据：分路文件（严格格式）成为合并目标并正确执行内源保留/跨源重叠判定，总线带后缀文件仍被拒绝——同一数据集上两类行为均验证通过
  - 产物命名 <用户号>-<最早>-<最晚>.csv，仅更新起止时间
- 是否进入 REPORT.md（稳定结论）：否（工程功能扩展）
- 遗留问题：无新增

## [2026-08-14] 专题修订④：官方点位表落地与缺列置 0 规则
- 类型：用户专题（修订）
- 目标与假设：总线三相 U/I/P/PF 按官方点位表映射 load_iden_dataN；文件中找不到对应列时日志提示并置 0。假设：置 0 不破坏下游流程（PF 重算、Scaler、模型训练），且不同设备文件列集合差异可用同一规则吸收
- 方法 / 数据 / 参数：
  - bus_field_map 更新为官方映射（ua/ub/uc/ia/ib/ic/pa/pb/pc/pfa/pfb/pfc → data9/45/81/1/37/73/7/43/79/8/44/80）
  - CsvBusLoader：映射列在文件中缺失 → WARNING + 置 0 + 报告标记 MISSING_COLUMN_ZERO_FILLED（非致命，不阻塞任务）
  - PF 重算兜底链：P/(U·I) 重算 → 失败回退文件 PF 均值 → 仍无则置 0
  - 新增 4 项测试；真实数据 5 用户复验
- 结果 / 结论：
  - 83 项测试全过；真实数据 10/10 OK；5 用户均缺 data9/45/81/37/44（三相电压+ib/pfb）被正确置 0 并告警
  - 官方映射优于临时猜测：800 用户 r2 0.716→0.762（data7 实为 pa 而非 ptotal）
  - 数据侧发现：当前数据集电压类点位整体缺失，电压/不平衡度特征暂无信息量，建议采集侧补齐（记入 TODO）
- 是否进入 REPORT.md（稳定结论）：否（待 M2 模型对比后再沉淀稳定结论）
- 遗留问题：电压类点位补齐；稀疏用户建模改善（M2）

## [2026-08-14] 专题修订⑤：总线电气量 /1000 倍率落地
- 类型：用户专题（修订）
- 目标与假设：实际 U/I/P/PF = 文件原始数据/1000；假设：加载器既有 multiplier 机制可配置化承载该规则，无需代码改动；均匀缩放下模型指标应近似不变（z-score 归一）
- 方法 / 数据 / 参数：
  - bus_field_map 12 字段 multiplier 统一 0.001；新增倍率测试（含 PF 归一断言）
  - 778 用户量级抽样验证 + 5 用户全量复验
- 结果 / 结论：
  - 84 项测试全过；真实数据 10/10 OK
  - 缩放后量级合理：ia≈0.756A、pa≈56.6W、pfa≈0.692（无量纲）
  - bus 质量分从缩放前（PF 越界异常）升至 98.7–100；模型指标不变（842/778/800 r2 0.624/0.827/0.762），符合不变性预期，无回归
- 是否进入 REPORT.md（稳定结论）：否（待 M2 模型对比）
- 遗留问题：电压类点位缺失（沿用）

## [2026-08-14] 专题：分类指标输出增加混淆矩阵计数 TP/FP/FN/TN + 全 5 用户重跑验证
- 类型：验证专题（含小型功能扩展）
- 目标与假设：在模型分类指标输出中增加 TP/FP/FN/TN 四类混淆矩阵计数（与 F1/Acc/P/R 同一 on_thr_w 二值化口径），并对全部 5 用户重新运行 train+infer 验证。假设：计数以注册表指标形式接入可零侵入贯通 metrics.json / comparison.csv/md / offline_metrics.json 全链路
- 方法 / 数据 / 参数：
  - metrics.py 注册 tp/fp/fn/tn 四个计数指标（复用 `_confusion`，per_branch=各分路计数，macro=跨分路总数）；default.yaml metrics 扩为 12 项
  - compare.py 新增 COUNT_METRICS 集合，summarize 排序时跳过计数指标（防止 FP=0 的退化全关模型被评为最优）
  - 新增 4 项测试（手算计数/多分路求和+四类守恒/阈值透传/排序豁免）；`run_batch_users.py --stage all` 全 5 用户重跑
- 结果 / 结论：
  - 96 项测试全过；批量执行 10/10 OK（train 5 + infer 5）
  - 守恒校验通过：各用户 TP+FP+FN+TN = test 样本数（1622/2/670/96/96）
  - best 模型测试集混淆矩阵（TP/FP/FN/TN）：842 ridge 662/374/23/563（F1 0.769）；844 proportional 0/0/0/2（test 仅 2 点全关，空真 F1=1 需谨慎解读）；778 history_profile 332/11/2/325（F1 0.981）；789 proportional 0/0/51/45（F1 0，稀疏用户 M2 待改善）；800 ridge 35/2/5/54（F1 0.909）
  - infer 离线混淆矩阵：842 1187/145/1/993（F1 0.942）、778 905/40/34/880（F1 0.961）；789/800/844 推理段 FN 全量（789 模型退化、800/844 推理期分路目标与训练期分布差异大），与既有 R2 基线结论一致
  - 各指标最优/综合最优模型挑选不受计数列影响（排序豁免生效）
- 是否进入 REPORT.md（稳定结论）：否（诊断输出扩展，非算法结论）
- 遗留问题：844 test 切分仅 2 点（时间过滤配置导致），分类指标统计意义弱；789/800 推理段 FN 全量的根因随 M2 多模型一并分析

## [2026-08-14] 专题：三阶段+日级评估指标输出与推理状态真值/概率扩列
- 类型：验证专题（含功能扩展）
- 目标与假设：①训练每模型输出 train/val/test 三阶段指标及每天日级指标 CSV；②推理结果增加状态真值（target_state）与开态概率（pred_prob），并输出日级指标 CSV。假设：日级分组评估可复用 evaluate_all 注册表机制；伪概率可在无分类头模型下提供有意义的置信度参考
- 方法 / 数据 / 参数：
  - evaluate_daily：按自然日分组调 evaluate_all，输出 date/n_points/各指标宏平均
  - state_probability：p=1/(1+exp(-4·(P−thr)/thr))，单调、阈值处 0.5、决策边界与 pred_state 一致
  - 产物：train/metrics_by_split.csv（model×split）、train/metrics_daily.csv（model×split×date）、infer/metrics_daily.csv（model×date）、inference_result.csv 扩为 7 列
  - 新增 5 项测试；真实数据 5 用户 --force 全量重跑
- 结果 / 结论：
  - 105 项测试全过；10/10 OK；5 用户产物全部齐备
  - 三阶段指标揭示过拟合幅度：842 ridge r2 train 0.954 / val 0.864 / test 0.616；proportional 三阶段 r2 全负（模型太弱非过拟合）
  - 日级指标定位问题天：842 ridge test 中 2026-06-12 r2=0、F1=0（全关日全误报），其余天 r2 0.5–0.8——整段指标掩盖的坏天被显式暴露
  - 推理侧语义校验全过：pred_prob 决策边界与 pred_state 一致（p≥0.5 ⟺ P≥thr）、target_state 与真值二值化一致、无真值处为空
- 是否进入 REPORT.md（稳定结论）：否（诊断能力扩展，非算法结论）
- 遗留问题：pred_prob 为伪概率（非校准），M2 分类头模型引入后可替换；指南附件「日级指标 23 字段」契约仍未提供，当前日级 CSV 按现有指标集输出，待附件到位后对齐字段

## [2026-08-14] 专题：M2 多模型落地（RF/XGB/LSTM/1D-CNN/Transformer）8 模型对比
- 类型：实验专题
- 目标与假设：新增随机森林/XGBoost/LSTM/1D-CNN/Transformer 五个回归模型，统一 BaseModel 接口接入 8 模型对比。假设：树模型在当前数据量（数千样本）上优于线性基线；DL 序列模型受样本量限制可能不占优
- 方法 / 数据 / 参数：
  - tree_models：RF（sklearn 原生 multioutput，n_est 200）、XGB（每分路一回归器，有 val 早停，n_est 400/depth 6/lr 0.05）
  - seq_models：_SeqTorchModel 适配器基类（L=96 滑窗 Seq2Point 逐点输出、头部复制填充、Adam+MSE、val 早停回滚最优权重、state_dict 级持久化、种子可复现）；LSTM(h64)/CNN1D(ch32×3)/Transformer(d64/head4/layer2)
  - 依赖惰性导入（未装 torch/xgboost 仅在实例化时报错）；requirements-ml.txt 增补 xgboost
  - 19 项新测试；真实数据 5 用户 --force 全量重跑（train+infer）
- 结果 / 结论：
  - 124 项测试全过；批量 10/10 OK；best_model 分布：842 ridge(0.616)、844 proportional、778 xgboost(**0.951**，原 best 0.827)、789 cnn1d(-0.767，全员负但 DL 最不差)、800 random_forest(**0.768**，原 0.666)
  - 树模型显著提效：778 r2 0.827→0.951（xgb），800 0.666→0.768（rf）——非线性+lag/rolling 特征组合有效
  - DL 三兄弟未超越树模型：样本量数千级不足以喂饱序列模型，且时序信息已被 lag/rolling 扁平特征吸收；789 例外（cnn1d 相对最好）提示强噪声用户下平滑归纳偏置有价值
  - 性能：树模型秒级；LSTM/CNN 分钟级；大用户 transformer CPU 约 25 min（可接受，后续可调）
- 是否进入 REPORT.md（稳定结论）：否（首轮对比，超参未调优，结论未稳定）
- 遗留问题：DL 调参与 GPU 加速；789/844 数据侧问题（无模型可救）；lightgbm(gbdt) 未实现（依赖已在 requirements-ml，需要时按同模式接入）

## [2026-08-14] 专题：GPU 自动检测 + 分路开机情况分析
- 类型：验证专题（含功能扩展）
- 目标与假设：①深度模型自动用 GPU（无 GPU 回退 CPU 不需改配置）；②训练/推理前对分路 CSV 做逐天开机分析（开机段起止/时长/最小/平均/峰值功率/电量/状态），整天关机输出整天一行 state=0
- 方法 / 数据 / 参数：
  - resolve_device('auto')：CUDA→MPS→CPU；显式 cuda 不可用回退并告警；predict 独立解析（跨设备加载）
  - analyze_branch_sessions：dropna 后按天分组、p>=on_thr_w 游程切段、电量 Σ(P·Δt)/1000 kWh、间隔中位差推断（5/15min 兼容）
  - 10 项新测试（手算单段/多段/整天关机/跨午夜/5min/NaN/设备解析）；真实数据 800 用户 train+infer 实测
- 结果 / 结论：
  - 134 项测试全过；真实数据 train 247 行（3 分路×71 天：118 开机段+129 全关天）——开机模式清晰（工作日 10:00–20:00 开机 9–11.5h，功率 80–272W，日电量 1.3–1.8kWh；p1 5/21–5/23 全关）
  - 统计量一致性（min≤mean≤max、state∈{0,1}、开机行 id≥1/全关行 id=0）全部通过
  - 本机无 GPU：auto→cpu 日志正确；monkeypatch 模拟有 CUDA：auto→cuda 正确
- 是否进入 REPORT.md（稳定结论）：否（工程功能扩展）
- 遗留问题：跨午夜会话按天切开（口径为"每天开机情况"）；如需"完整会话"视图可后续加 session 合并选项

## [2026-08-14] 专题：7 模型批量验证 + 日级指标达标分析（SAE<0.2 且 F1>0.9）
- 类型：验证专题 + 数据分析专题
- 目标与假设：注释 transformer 提速批跑；按临时达标口径（SAE<20% 且 F1>90%）逐模型逐天判定，对不达标日的用户数据归因
- 方法 / 数据 / 参数：
  - default.yaml models 临时注释 transformer（CPU 大户 ~25min）；--force 全量重跑（7 模型×5 户 train+infer）
  - 新增 scripts/analyze_daily_metrics.py：扫描最新 metrics_daily.csv → 达标判定+原因归类（全漏报/全误报/全关日/SAE 或 F1 单项）→ 落盘 outputs/analysis/{daily_metrics_compliance,daily_metrics_summary}.csv
  - 交叉验证：不达标日关联 branch_sessions.csv 开机特征 + inference_result 的 Σpred/Σtarget 系统偏差
- 结果 / 结论：
  - 批跑 10/10 OK，总时长 ~13min（transformer 注释后从 ~50min 降为 1/4）；134 项测试全过
  - 总计 346 行（模型×天，test+infer），不达标 277 行（80.1%）；原因分布：仅 SAE 超标 90、SAE+F1 双超 61、全漏报 56、全误报 38、仅 F1 不足 32
  - **逐用户归因**：
    - 842（best ridge，test 18/21 不达标）：①6-12/6-15 真值全关但模型全误报——训练集全关日占比低，模型没学会"停机模式"；SAE 出现 1e13 天文数字=真值 0 电量做分母（口径缺陷）；②多数天 SAE 0.2–0.42 轻超+F1 0.78–0.90 略欠——FP 偏多（开机边界过宽）；infer 期 8/25 不达标同模式（Σpred/Σtarget=0.86 系统低估 14%）
    - 844（test 仅 1 天无意义）：infer 28/28 全不达标——7-01~7-04 真值全关日 SAE 分母为 0（假性超标，F1 实际=1.0）；7-05 起全漏报（训练期 target 分布与推理期完全脱节，模型只会输出 0）——数据侧问题
    - 778（best xgboost，test 7/7 全达标✅）：infer 仅 7/20 不达标且全部是"仅 SAE 超标"（0.22–0.29 轻微超线，F1 0.99 完美）——Σpred/Σtarget=0.83 系统低估 17%：推理期（7 月）负荷水平较训练期（5–6 月）上移，模型幅值跟不上但开关判定极准
    - 789（best cnn1d）：test/infer 全不达标——F1 恒 0.68–0.72 且 TN=0（模型永远预测开机，真值也几乎全开但有 45% 时段实际低于阈值）；on_thr_w=60 的二值化把介于 40–60W 的"半开"状态判为关，模型无法区分——阈值与负荷形态不匹配+数据可辨识性差（该户 r2 全员负）
    - 800（best rf，test 1/1 达标）：infer 28/28 不达标但全部"仅 SAE 超标"（F1 0.96–0.99 完美）——Σpred/Σtarget=0.57 系统低估 43%：推理期功率量级明显高于训练期（训练仅 5-27~6-01 共 6 天，代表性不足）
  - **共性结论**：①开关状态判定（F1）树模型已很强，主要缺口在幅值回归（SAE）——训练期与推理期负荷水平漂移是 SAE 超标主因，需扩充训练时间范围覆盖季节变化；②真值全关日的 SAE 分母为 0 导致假性天文数字——建议达标口径对全关日只考核 F1（或 SAE 分母加保护）；③789 需重新评估 on_thr_w 或先解决可辨识性
- 是否进入 REPORT.md（稳定结论）：否（临时达标口径，正式口径待需求方确认）
- 遗留问题：①SAE 全关日分母保护（口径修订建议）；②842/800 训练时间范围扩充；③789 阈值/可辨识性专项；④transformer 恢复时机（GPU 环境或调参后）

## [2026-08-17] 专题：逐用户×逐模型日级指标详析 + F1 不达标日重点归因
- 类型：数据分析专题
- 目标与假设：对 7 模型×5 用户的每日评估指标全景分析；对 F1≤0.9 的天做混淆矩阵形态学归类与数据交叉验证
- 方法 / 数据 / 参数：
  - 沙箱重置后重建环境+全量重跑（10/10 OK，结果与 08-14 完全一致：346 行/277 不达标，可复现性验证通过）
  - F1 不达标日按混淆矩阵形态六分类：A 全漏报（预测全关真值有开）/ B 全误报（真值全关预测有开）/ C 错位 / D 过度预测开（TN=0）/ E 漏报为主 / F 误报为主
  - 交叉数据源：branch_sessions.csv（真值开机形态）、cleaned CSV（训练分布）、inference_result.csv（逐点 pred vs target）、meta.json（scaler/切分）
- 结果 / 结论：
  - **形态×模型分布揭示三类根因**：
    1. **A 全漏报 47/56 行来自 proportional——确认为工程缺陷而非数据问题**：proportional 用特征列 pbus×share 预测，但 pbus 在 scale_cols 中被 z-score 标准化（844 户 μ=294.7/σ=322.5），标准化后中位≈0，clip(0) 后预测恒≈0 → 5 户 test 全期 TP 合计=0（除空真外 F1 恒 0）。**该基线自 Scaler 引入后即失效，历史对比中 proportional 的"胜出"（844/789 best）全部是空真/退化假象**
    2. **D 过度预测开（TN=0）50/50 行来自 cnn1d，29 行集中在 789**：789 真值 54% 时段 <60W（阈值），但分布双峰（P50=32W vs P75=1312W），阈值恰好切在低峰上；cnn1d 全局平均池化的平滑偏置使 pred 恒在 371~610W（pred<60 占比=0）→ 永远报开机。真值"开"占比 50~56%，故 F1 卡在 0.67~0.72。**根因=阈值 60W 与双峰负荷形态不匹配 + 模型无低功率分辨力**
    3. **B 全误报 32 行集中在 842**：训练集 150 天中全关天仅 17 天（11%），且全关天 pbus 中位 237W 与开机天 296W 高度重叠——模型（全员）无法从总线水平区分"目标分路停机"，6-12/6-15/6-23 等停机日全误报。**根因=停机日样本少且总线信号无区分度（其他分路仍在用电）**
  - **逐用户 F1 详析**：
    - 842（best ridge）：test 21 天中 F1 不达标 12 天——B 形态 5 天（上述停机日）+ F 形态 7 天（F1 0.78~0.90，FP 13~23 点/天，开机边界判宽约 3~5 小时）；infer 25 天仅 4 天 F1 不达标（0.85~0.87，同 FP 边界问题，程度更轻）
    - 844（best proportional）：infer 7-05 起 24 天全漏报 F1=0——proportional 缺陷（上述①）叠加该户真值 on 占比 31%~53% 正常但 pred 恒 0；train/infer 真值分布其实高度一致（P95 765 vs 762W），**此户模型层面本可救，被基线缺陷掩盖**
    - 778（best xgboost）：F1 全期 100% 达标（test 7 天 + infer 20 天 F1≥0.938），当前标杆
    - 789（best cnn1d）：29 天全部 D 形态（上述②）；tree 模型 F1 0.81~0.84 反而高于 cnn1d 0.72，但 SAE 更差——若正式口径重 F1，789 的 best 应换 xgboost
    - 800（best rf）：infer 28 天中 F1 不达标 5 天，全部在 7-23 之后——7-29~31 真值全关但 pred P50 76~81W>50W 阈值持续误报；总线 7-29~31 中位 20~26W 与 7-20~22（18~20W）几乎无差，**目标分路停机但总线仍有底载，模型（训练期仅 6 天且无停机日样本）没见过"低总线+停机"模式**
  - **F1 值分层**：F1=0 的 94 行中 47 行 proportional 缺陷、24 行 844 连带、其余为真实停机日误报；0.7~0.8 带 22 行几乎全是 789 阈值问题——**修掉 proportional 缺陷后真实 F1 问题集中在：停机日辨识（842/800）与阈值适配（789）两类**
- 是否进入 REPORT.md（稳定结论）：否（proportional 缺陷待修复后重跑验证）
- 遗留问题：①【高优先】修复 proportional 基线（应使用未缩放 pbus：方案 A 模型内反标准化，方案 B pbus 移出 scale_cols，方案 C pipeline 传原始 pbus——倾向 B，slot 先例已存在）；②789 建议 on_thr_w 复核或 best 换 xgboost；③842/800 停机日辨识需要停机样本增强或"总线底载"特征；④SAE 全关日分母保护（沿用上轮建议）

## [2026-08-17] 专题：数据质量报告统计数据正确性复核
- 类型：验证专题
- 目标与假设：对质量报告全部统计量做独立复核——用与实现不同的代码路径（逐天循环、正则功率列、手写四项指标公式）从 cleaned CSV 重放管线重算，与产物 JSON/HTML 逐项对比
- 方法 / 数据 / 参数：
  - 复核对象：789 户（干净数据基线）+ 842 户（含缺失天/剔除天复杂场景，train+infer 双侧）
  - 覆盖统计量：n_rows / n_days_approx / missing_rate / outlier_rate / coverage_rate / quality_score / cleaned_stats 六字段（total/actual/missing/missing_dates/all_off/all_off_dates）× bus/branch/branch_target 三报告 × split_stats（train/val/test/infer）
  - 交叉一致性：HTML 表格数值 vs meta.json；excluded_days vs missing_dates；split_stats.total_days vs metrics_daily 天数；剔除天∩评估天=空；全关∩缺失=空
- 结果 / 结论：
  - **全部通过，0 不一致**：789 户 36 项逐项相等；842 户 train 侧 cleaned_stats（bus 132/129/3、branch 132/131/1、全关 8/16 天清单逐日相等）、split_stats（58/19/21 天与 metrics_daily 完全一致）、HTML 6 行与 JSON 逐值相等；infer 侧（118 天/全关 7/split_stats.infer 25 天/missing_rate 0.455266/coverage 0.3026）全部相等
  - 交叉一致性全部成立：训练窗内 bus 缺失天 ⊆ excluded_days（窗外 7-29 不需剔除，语义正确）；剔除天不出现在任何评估天；全关与缺失严格互斥
  - **两个口径疑点核查后确认为设计而非缺陷**：①日历缺口天（跨度内整天无行，842 有 254 天）不计入 total_days/missing_days——total_days 口径=「有数据行的天」，缺口由 n_days_approx−total_days 与 coverage_rate 反映（跨度 386−132=254 自洽）；②excluded_days ⊉ 全部 missing_dates 是因为时间过滤窗外的缺失天无需剔除（例：7-29 在 train 窗 2025-07-10~2026-06-30 之外）
- 是否进入 REPORT.md（稳定结论）：否（复核性验证）
- 遗留问题：无——如需把「日历缺口天」也显式列出，可在 cleaned_stats 增加 gap_days 字段（当前由覆盖率反映，暂不加）

## [2026-08-17] 专题：清洗插值泄漏缺陷修复（2842 用户 6-13 全天缺失天误判问题）
- 类型：验证专题（缺陷修复）
- 目标与假设：用户报告 2842 目标通道 2026-06-13 清洗前为全天缺失天、清洗后不再是。假设：清洗的插值步骤向全缺失天泄漏了伪有效点
- 方法 / 数据 / 参数：
  - 复现：清洗前 6-13 p1 全 96 点 NaN；清洗后出现 2 个非 NaN 点（00:00/00:15，值 0.0，来自 6-12 尾部 0 值的插值延伸）
  - 根因：pandas `interpolate(limit=N)` 的语义是「每个缺口最多填 N 个点」——长缺口仍被**部分填充**前 N 个点，与设计意图「超过 N 的缺口整段保留 NaN」不符；全天缺失天因此被"漏"进 max_gap_interp 个伪点，污染缺失天统计与无效天判定（96 点日缺失 94 点=97.9%>90% 仍会被剔除，但 288 点粒度或更低阈值场景会漏网；且缺失天统计已然错误）
  - 修复：`_interp_short_gaps`——按缺口游程长度整段决策（≤max_gap 全补 / >max_gap 全不补），并用 `limit_area="inside"` 禁止首尾外推
  - 验证：新增 tests/test_clean.py 6 项（短缺口全补/长缺口零填充/尾部不外推/头部不填/端到端 2842 场景回归/混合缺口）；全 5 户 --force 重跑更新质量报告
- 结果 / 结论：
  - 152 项测试全过；修复后 6-13 p1 非 NaN 点数=0 ✅
  - 842 目标通道统计归位：缺失天 1→6 天（6-13/6-14/6-20/6-22 等此前被伪点污染的天全部归位），全关天 16→11 天（伪 0 值天不再计入全关）；6-13 正确进入缺失清单+剔除清单，不在全关清单、不在日级评估 ✅
  - 全 5 户重跑：842/778/789/800 train+infer OK（8 项），统计一致性守卫（actual=total−missing、全关∩缺失=空）全过；844 仍 INSUFFICIENT_TIME_RANGE（既有数据问题，与本修复无关）
  - 影响面评估：插值泄漏同样影响过 bus 侧（u/i/pf 列同样被部分填充）——修复对所有数值列生效；模型指标不受显著影响（伪点占比极小），但日级统计/无效天判定的正确性依赖本修复
- 是否进入 REPORT.md（稳定结论）：否（缺陷修复记录）
- 遗留问题：无

## [2026-08-17] 专题：2842 用户 3 模型训练 F1 不达标（>95%）详析与优化方案
- 类型：实验专题（分析）
- 目标与假设：解剖 3 基线模型 test F1（hp 0.631/prop 0.000/ridge 0.750）距 0.95 的缺口结构，定位可优化路径
- 方法 / 数据 / 参数：混淆矩阵结构分解、FP 时空解剖（时段/日分布/预测值分位）、阈值敏感性扫描、后处理参数扫描、状态分类器与日级门控离线可行性实验（不入库）
- 结果 / 结论：
  - **缺口结构：recall≈1.0、precision 0.46~0.61——FP 是唯一瓶颈**（ridge test：TP 809/FP 515/FN 24）。真值无灰区（开态 P5=207W，关态 97% 为 0，5~20W 点占比 0），阈值 10W 本身合理
  - **FP 三源**：①全关天白天整段误报 50%（6-12/15/23/29 四天各 ~48 点，模型从总线看不出目标停机——全关天白天 pbus P50 304W vs 开机日 401W，62% 重叠）；②开机边界扩张（早/晚各判宽 1~4 点）；③夜间小值抖动（0-7 时真值恒关但 lag/rolling 特征残留，FP 预测值 33% <30W）
  - **prop=0 的另因**：proportional×Scaler 缺陷（已知，见 08-17 专题）；hp 的 FP 更重（画像把历史均值摊到每天，全关天全天误报）
  - **优化实验（ridge test F1 路径）**：决策阈值 50W→0.849；+min_on=8→0.874；夜间置关/每日单段增益≈0（与 min_on 重叠）；状态分类器割点 0.5→P 0.90 但 R 崩至 0.63（F1 0.745）；日级门控单变量重叠严重不可行
  - **天花板判断：点级/后处理手段上限 ~0.87-0.88**。剩余 199 FP 中 193 点集中在 4 个全关天——这些天总线水平正常（其他分路在用电），**目标分路停机在总线上无可观测信号，是可辨识性下界而非模型缺陷**。要破 0.95 必须引入停机可观测特征（目标分路专属电气量/工单日历）或接受「全关天白天误报」的固有风险
- 是否进入 REPORT.md（稳定结论）：否（单用户分析；结论供 M2 调参与口径讨论）
- 遗留问题：优化方案落地待确认（后处理参数是用户级配置，改 time_filters.json 即可；模型侧样本加权/分类头需开发）

## [2026-08-18] 专题：无效分路通道语义落地 + 2842 复验与 F1 不达标天详析
- 类型：验证专题（语义修正 + 分析）
- 目标与假设：业务确认「配置目标通道=唯一有效分路，其余通道不在当前总线回路（无效数据）」。检查代码、修正后在 2842 复验，并详析 F1（>95%）不达标天
- 方法 / 数据 / 参数：
  - 代码审计发现：目标构建/评估已只用目标通道✅；但清洗产物 branch_cleaned.csv、整表 branch 质量报告仍含无效通道 p2/p3/p4（既有三层口径中的「全景参考」在新语义下失效——无效通道数据无业务意义且误导）
  - 修正：train/infer 两侧在清洗+目标解析后立即丢弃非目标通道（log 提示）；branch 质量报告=有效通道口径（branch_target 子表报告取消并入 branch，split_stats 挂 branch）；无效天判定/开机分析/清洗产物随之自然只见有效通道
  - 152 项测试全过（断言更新：cleaned branch 列=target_cols、HTML branch·train/infer）；2842 --force train+infer 复验
- 结果 / 结论：
  - 复验通过：丢弃 p2/p3/p4 日志正常；branch_cleaned.csv 只含 p1；branch 报告 总146/实际140/缺失6/全关18；三阶段指标与修改前逐位一致（目标构建本就只用 p1，语义修正不改变模型结果——符合预期）
  - **F1≤0.95 共 19/21 天，四类归因**：A 全关天整段误报 4 天（FP 257=总 FP 51%，6-12/15/23/29，真值峰值 0W 但模型整白天误报——总线无停机信号，可辨识性下界）；B 误报为主 6 天（FP 20~32/天，开机日误报段过长）；C 边界轻误报 7 天（FP 6~16，开机边界扩张 1.5~4h）；D 其他 2 天（6-08 低功率日 106W 峰值难判、6-25 数据半天 FN8）
  - 达标仅 2/21 天（F1 0.958/0.972 均为长开机日+清晰边界）
- 是否进入 REPORT.md（稳定结论）：否
- 遗留问题：优化方案见本专题下方（数据集/模型两侧）；A 类天花板问题沿用 08-17 结论

## [2026-08-18] 专题：阈值 70 + 2842 排除不达标天重验 + F1 不达标分析与优化
- 类型：验证专题 + 分析
- 目标与假设：min_score 10→70；2842 训练/推理窗显式 exclude 阈值 70 下的不达标天；重验并详析 F1（>95%）不达标天
- 方法 / 数据 / 参数：
  - default.yaml quality.min_score=70；离线预演逐天质量表定位不达标天——训练窗 12 天（bus 低分 5 天：7-15/7-17/7-29/4-17/4-18；分路全缺 4 天：6-13/14/20/22；分路半天缺 3 天：6-15/24/25）+ 推理窗 3 天（7-23/29/30，bus 低分）
  - time_filters.json 2842 train/infer 增加 exclude（连续天合并区间）；--force 重跑 train+infer
- 结果 / 结论：
  - 160 项测试全过；train+infer OK。切分 6098/2092/2013；双达标 131 天（全关 17：训练 64/验证 22/测试 21/未使用 24）
  - **exclude 生效验证**：训练/评估不再含不达标天（残留于逐天表的 15 天为报告全景展示，非训练数据）；推理侧 F1=0 的天全部消失（原 7-23/29/30 半天数据反复产生 0 分行）
  - 指标变化：test ridge F1 0.750→0.761、离线推理 F1 0.930→0.908（7-05 等真值全关天原不在评估、现进入正常波动）——**排除不达标天提升的是指标可信度而非绝对值**
  - **F1≤0.95 归因（test 20/21 天）**：A 全关天整段误报 4 天（6-01/12/23/29，FP 257=51%，可辨识性下界）；B 误报为主 6 天（FP 15~31）；C 边界轻误报 10 天（F1 0.86~0.94，FP 7~16）。推理侧 19 天全部为 B/C 型（FP 7~23，无 A 型——推理期无全关天进评估）
  - **优化量化（test）**：现状 0.764 → 决策阈值 50W：0.848 → +min_on=8：**0.881**（FP 500→199）；**仅开机日口径（排除 4 全关天）+同参数：F1=0.982 ✅ 达标**——证明 0.95 口径在含全关天时不可达（模型无停机信号），排除后完全可达
- 是否进入 REPORT.md（稳定结论）：否
- 遗留问题：①后处理参数（post_min_on=8/决策阈值 50）建议写入 2842 配置正式生效；②全关天口径与需求方协商（单独考核或排除）；③停机日历/目标通道专属电气量特征（结构性方案）

## [2026-08-18] 专题：2842 优化方案 1+2 落地验证（decision_thr_w+post_min_on / 开机日口径）
- 类型：验证专题（优化落地）
- 目标与假设：方案1（后处理参数写入配置正式生效）+ 方案2（开机日口径 F1 正式产出），验证 F1>0.95 可达性
- 方法 / 数据 / 参数：
  - 新增用户配置字段 decision_thr_w（CONFIG_RULES，默认 None=沿用 on_thr_w）：仅作用于预测→状态判决（pred_state/pred_prob/状态策略评估）；真值判态与分类指标恒用 on_thr_w（口径不破坏）
  - 训练新增产物 state_strategy_metrics.csv：每模型 test 段在 decision_thr+min_on 后处理下的 F1/P/R/混淆计数 × 两口径（all_days / on_days_only）
  - 2842 配置：post_min_on=8、decision_thr_w=50.0；161 项测试全过（新增 1 项端到端）
- 结果 / 结论：
  - **训练 test（ridge）**：全量口径 F1 0.881（P 0.806/R 0.972，FP 200）；**开机日口径 F1 0.9823 ✅ 达标**（FP 仅 6 点/17 个开机日）
  - **推理 pred_state（优化后处理生效）**：**F1 0.981**（P 0.965/R 0.997，FP 43/FN 3），对照原始口径 0.908；日级 24 天中 **22 天 F1>0.95**（中位 0.985），仅 7-27（0.928，午后 6 点 95~302W 疑似真值漏记）与 7-14（0.949，晚间 4 点边界）未达
  - 全量口径剩余 FP 结构：4 个全关天贡献 FP 257（6-01/12/23/29）——占绝对多数，可辨识性下界结论不变
- 是否进入 REPORT.md（稳定结论）：建议下轮进入（优化方案实证有效且已配置化）
- 遗留问题：①7-27 午后 FP 段疑似分路真值漏记（连续 6 点 95~302W 预测但真值 0），建议数据侧核查；②全关天口径正式协商（all_days 0.88 vs on_days 0.98 两个数字并出，由需求方选择考核口径）

## [2026-08-18] 专题：三份评估产物指标不一致分析（by_split/daily vs state_strategy）
- 类型：验证专题（口径审计）
- 目标与假设：解释 metrics_by_split.csv / metrics_daily.csv 与 state_strategy_metrics.csv 的 F1 等指标为何不同；排查是否存在真实缺陷
- 方法 / 数据 / 参数：2842 最新产物三方对比 + 同一份 ridge test 预测重放两条评估链逐步分解（阈值/游程/口径三因素隔离）
- 结果 / 结论：
  - **差异主因是设计口径不同（预期行为）**：①metrics_by_split/metrics_daily = 「模型能力口径」——原始预测 ≥ on_thr_w(10W) 直接判态、无后处理（跨模型/跨用户可比，选型口径）；②state_strategy = 「生产判决链口径」——decision_thr_w(50W)+min_on(8)+fill(3) 后处理后的状态（部署效果）。同一预测重放：10W 无后处理 →(0.764,FP 500)；50W→(0.858,FP 250)；+min_on8→(0.881,FP 199)，逐步分解完全复现两份产物差异
  - **metrics_daily 与 by_split 是同口径**（日级 tp/fp/fn 求和=整段 837/507/18）；日级 F1 简单平均 0.70 低于整段 0.76 属正常（小样本天 F1=0 拉低平均，不可直接比较）
  - **发现并修复一个真实缺陷**：state_strategy 的 precision 空真约定与 evaluation.metrics 不一致——tp+fp=0 且有 FN 时误记 1.0（proportional 全漏报却显示 precision=1.0），已改为记 0
  - **改进**：state_strategy_metrics.csv 增加 strategy 列输出两行——raw_on_thr（与 by_split 完全同口径的对照行，测试断言逐值对账）+ decision+runs（生产链）；三份产物从此可直接互查
- 是否进入 REPORT.md（稳定结论）：口径设计说明建议纳入
- 遗留问题：无

## [2026-08-18] 专题：proportional 在 2842 三阶段 F1 全 0 根因分析与修复
- 类型：验证专题（缺陷修复，闭环 08-17 高优先 TODO）
- 目标与假设：解剖 proportional train/val/test F1 恒 0 的机制并修复
- 方法 / 数据 / 参数：train_predictions.csv 取证（预测分布）+ meta.scaler 状态 + 机制复算三证据链
- 结果 / 结论：
  - **根因（三证据闭环）**：①pred_proportional 全量 <3.6W（P95=1.39），≥10W 阈值占比 0 → TP 恒 0；②pbus 在 scale_cols 中被 z-score 标准化（μ=249W/σ=181 → 标准化后中位≈0）；③proportional 的 pred=share×z(pbus)（单目标 share=1），clip(0) 后只剩 0~3.5 的正 z 尾巴——**模型假设「pbus 列是物理功率」被 Scaler 破坏**，与数据质量无关
  - 连锁澄清：precision 空真行（上轮已修）曾使该缺陷更隐蔽（全漏报却显示 precision=1.0）
  - **修复（方案 B）**：pbus 加入 NON_SCALED_COLS（slot 先例，物理量纲直通）；ridge 等模型个别列不缩放数学无碍（实证 ridge test f1 0.7613→0.7602、r2 0.6051→0.6046，差异在切分随机性内）
  - **修复后 proportional 复活**：test MAE 173.7（优于 history_profile 253.5）、R² 0.488、SAE 0.093、F1 0.596（recall 1.0/precision 0.425）——回归其「粗但不瞎」的基线定位；F1 仍不高属模型本性（占比分摊把夜间底载也放大为开机误报），非缺陷
  - 163 项测试全过（新增回归守卫：pbus 不在 scale_cols + proportional 预测物理量级 + TP>0）
  - 影响面：789/844 历史 best=proportional 的结论作废（退化假象），下轮全量重跑后重新选型
- 是否进入 REPORT.md（稳定结论）：修复事实建议记入
- 遗留问题：全量 5 户重跑刷新 best_model（待批跑窗口）；proportional 的 F1 上限受其模型本性限制，不再投入调优

## [2026-08-18] 专题：train_predictions vs metrics_daily 混淆矩阵计数不一致分析
- 类型：验证专题（口径审计续篇）
- 目标与假设：解释 train_predictions.csv 的状态列与 metrics_daily.csv 的 TP/FP/FN/TN 数量差异；确认是否有缺陷
- 方法 / 数据 / 参数：2842 最新产物三口径对比（daily 求和 / 值列@on_thr 判态 / 状态列）+ 逐因子分解（阈值→游程）
- 结果 / 结论：
  - **无缺陷，差异=双口径设计的直接体现**：
    - metrics_daily（=metrics_by_split 同口径）：**pred 值 ≥ on_thr_w(10W) 直接判态、无后处理**（模型能力口径）——ridge test 求和 837/510/18/648
    - train_predictions 的 pred_state 列：**decision_thr_w(50W)+min_on(8)/fill(3) 游程后处理**（生产判决链口径）——831/200/24/958
  - **精确对账验证**：用 train_predictions 的 pred 值列按 on_thr_w=10 重新判态 → 837/510/18/648 与 metrics_daily **三模型逐值相等** ✅（两份产物同源同预测，只是判决链不同）
  - 逐因子分解（ridge test FP）：510 →（阈值 10→50 消夜间小值）→ 250 →（min_on=8 消短段）→ 200；TP 837→831（阈值损失 6 个 10~50W 弱开机点）
  - 三口径样本数均为 2013（无样本集差异）；proportional 的 daily TN=0 vs 状态列 TN=370 同因（其预测恒>10W，但 50W 阈值+游程能判出部分关态）
  - 文档澄清：README train_predictions 条目增加口径提示（如何对账）
- 是否进入 REPORT.md（稳定结论）：与上轮口径设计说明合并考虑
- 遗留问题：无——如需状态列与 daily 完全一致，将用户 decision_thr_w/post_min_on 配置移除即可（缺省时两口径自动重合）

## [2026-08-18] 专题：三模型每日指标 FP 过多的模型侧治理（加权岭/中位画像）
- 类型：实验专题（调参落地）
- 目标与假设：在既有后处理手段（decision_thr+min_on，压推理链 FP）之外，从模型本身减少原始预测的 FP（metrics_daily 口径）
- 方法 / 数据 / 参数：
  - 离线扫描（2842 test，on_thr=10 口径）：ridge alpha 5~50 无效（FP 481→495~511，正则不改变系统性高估）；**关态样本加权 w_off=2/3/5/10 → FP 418/376/342/321、F1 单调升至 0.824**；history_profile 中位画像 FP 363→184（P25 过狠 FN+311 弃用）
  - 落地：ridge 新增 off_weight/off_thr_w 参数（加权岭闭式解 X'ΩX，默认 1.0=原行为）；history_profile 新增 agg=mean/median 参数；default.yaml 启用（ridge off_weight=5，hp agg=median）
  - 顺带修复既有缺陷：history_profile 的 unseen 槽位判定用 profile==0 代理——合法 0 值画像（median 下大量出现）被误回退 fallback；改为显式 _seen 数组（兼容旧 pickle）
  - 新增 2 项测试（加权压关态残余/off_weight=1 等价原版；median 语义/非法 agg 报错），165 项全过
- 结果 / 结论（2842 test，metrics_daily 口径）：
  - history_profile：FP 1054→184（-83%），F1 0.619→0.859（代价 FN 0→73）
  - ridge：FP 510→395（-23%），F1 0.760→0.799，SAE 0.128→0.028 显著改善；生产判决链 all_days 0.877/on_days 0.976 维持
  - proportional：不调（其 FP 来自模型本性——占比分摊无法区分底载，且作为 sanity 基线应保持朴素）
  - **FP 治理全景（三层）**：①模型层（本轮：加权岭/中位画像，压原始预测 FP）→②判决层（decision_thr_w+post_min_on，压生产链 FP，前轮已落地）→③口径层（on_days_only，处理全关天可辨识性下界）；三层叠加后 ridge 生产链 F1 0.88（全量）/0.98（开机日）
- 是否进入 REPORT.md（稳定结论）：建议与前两轮 FP 治理结论合并成章
- 遗留问题：全关天整段误报仍是模型层无解项（需停机可观测特征）；787/844 等其他用户的参数需按各自数据校准（off_weight/agg 均为用户级可配）

## [2026-08-18] 专题：压 FP 调参后 2842 复验与日级 F1 不达标再分析
- 类型：验证专题
- 目标与假设：加权岭/中位画像落地后重跑 2842，复盘日级 F1（>0.95）不达标天结构变化与剩余问题
- 方法 / 数据 / 参数：--force train+infer 重跑（165 项测试全过）；raw（on_thr=10 无后处理）与生产判决链（decision 50+min_on8）双口径逐日分析；FP 位置分解（开机前 2h/停机后 2h/远离边界）
- 结果 / 结论：
  - **达标数大幅改善（test 21 天，F1>0.95）**：hp raw 口径 0→12 天（中位 0.958）；ridge raw 6 天（中位 0.895）→ **判决链口径 15/21 天（中位 0.980）**；推理 24 天达标 11（中位 0.944，raw 口径）
  - **ridge raw 口径剩余 15 个不达标天两类**：①4 个全关天 F1=0（FP 242，可辨识性下界，不变）；②11 个 B/C 天 FP 合计 132——位置分解：边界扩张 58 点（开机前/停机后 2h 内）+ **远离边界 74 点全部集中在凌晨 1~6 时**（10~50W 小值抖动）
  - **判决链口径下 B/C 类几乎清零**：同样 11 天在 pred_state 口径 F1 0.91~1.00（六天抽查 FP≤1）——凌晨小值被 50W 决策阈值消掉、边界短段被 min_on 消掉；判决链口径不达标只剩 4 全关天 + 2 个 FN 天（6-10 FN9/6-08 FN6，开机段内低功率间歇被阈值误杀）
  - **结论**：raw 口径的 B/C 类"不达标"不代表生产问题（判决链已治理）；真正待解仍是全关天（模型层无解）与 6-10/6-08 类段内低功率间歇（决策阈值的 R/P 权衡代价）
- 是否进入 REPORT.md（稳定结论）：三层治理效果数据建议随前几轮合并成章
- 遗留问题：①全关天需停机可观测特征/口径协商（不变）；②若 FN 敏感可将 decision_thr_w 50→30 回调换 R（FP 会回升，需求方权衡）；③推理侧日级仍按 raw 口径输出——如需生产口径日级达标统计，可扩展 metrics_daily 增加判决链行（待需求确认）

## [2026-08-20] 专题：全关天严重误报根因终判——目标设备功率未计入总线（TARGET_NOT_VISIBLE_ON_BUS）
- 类型：验证专题（根因升级 + 检测落地）
- 目标与假设：解剖 hp/ridge 对全关天整段误报的机制；此前结论「总线无停机信号」升级为可量化的物理根因
- 方法 / 数据 / 参数：预测形态取证、画像数学结构分析、输入可分性实验、开机沿跳变比对、日级门控/夜间基线差/早晨阶跃三组缓解实验
- 结果 / 结论：
  - **决定性证据（开机沿分析，115 个开机沿）**：p1 跳变中位 723W，同时刻 pbus 仅跳 92W——**总线可见性比值 0.12**。目标设备功率基本未进入总线计量。物理解释：bus 的 B 相全缺（ub/pb=data45/43 缺列置 0），p1>pbus 的点占 93%（比值中位 1.59）——**目标设备大概率挂在缺失计量的 B 相**
  - 机制分解：①hp 画像模型只依赖 slot 对当天输入零感知，白天槽位历史中位 712W → 全关天必然输出 709W 整段误报（结构性，mean/median 皆然）；②ridge 依赖总线，但目标开机在总线上只有 92W 弱信号，叠加背景负荷波动（±100W 量级）完全淹没——全关天白天预测 143~598W
  - **三组缓解实验全部失败（量化确认不可救）**：7 特征日级门控（cut=0.7 仍误杀 8 个开机日）；Δ(白-夜)基线差（开机日 P10=273 vs 全关天 P90=333，重叠）；早晨最大阶跃（全关天 65% 超开机日 P10）——弱信号被背景波动淹没，任何总线侧特征都不可分
  - 全关天无日历规律（分散在周一~周日），排除星期特征方案
  - **工程落地**：可辨识性分析新增「总线可见性」检测——开机沿 ≥10 个时计算 bus 跳变/target 跳变中位比，<0.5 标记 TARGET_NOT_VISIBLE_ON_BUS 并判 identifiable=False。2842 实测比值 0.1338 精确命中自动告警（此前 pearson 0.835 掩盖了该问题——白天形态相关≠幅值计入）；新增 1 项测试（不可见/可见对照），166 项全过
- 是否进入 REPORT.md（稳定结论）：**建议进入**——「2842 全关天误报不可由算法解决，需数据侧修复 B 相计量（data45/43 点位）或提供停机日历」是稳定结论
- 遗留问题：①向采集侧确认 B 相点位缺失与目标设备接相；②若 B 相计量补齐，全关天误报预期自然消失（总线将出现 700W 级停机落差）；③其余 4 户的 bus_visibility_ratio 待批量体检

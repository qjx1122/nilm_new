# BOOTSTRAP.md — Agent 会话与任务协议（NILM_AC）

> **用法**：每次新 session 开头发一句「读 BOOTSTRAP.md 并按规则工作」即可。
> **核心思想**：思维链不依赖对话记忆——开局从文件恢复现场，收尾把进展固化到文件。**仓库 + 工作区文件是唯一事实来源。**
> **核心约束**：会话纪要、专题报告一律只写入指定文件（见下表）、只追加不新建，避免文件爆炸、管理混乱。
> **角色约束**：角色（人设 / 行为约束 / 输出偏好）必须落盘到 `ROLE.md`，由 `STATUS.md` 的「当前角色」指定生效者——角色不留在对话里。
>
> 协议版本：**v2.2**（2026-09-09 专题模板补「用户执行命令」字段；修订记录见文末）

---

## 📚 指定文件台账

| 文件 | 用途 | 写入规则 |
| --- | --- | --- |
| `STATUS.md` | 续接文件：当前角色 / 当前目标 / 已完成 / 进行中 / 下一步 / 决策记录 / 关键路径 | 每次收尾必更新；不存在时按文末模板创建 |
| `ROLE.md` | 角色定义库：角色名 / 性格风格 / 知识背景 / 职责边界 / 输出偏好 / 约束；`STATUS.md`「当前角色」指向其中定义 | 仅角色体系稳定时更新；所有角色收敛在同一文件，不拆多文件；不存在时按文末模板创建 |
| `session/NILM_AC_session_complete.md` | 会话纪要 | 只追加，每 session 一条，不新建文件 |
| `REPORT_TEST.md` | 专题报告（用户 / 实验 / 验证专题） | 只追加，按专题分节，不新建文件 |
| `REPORT.md` | 稳定结论：算法路线 / 稳定实验结论 / KPI 口径 / 推荐稳定版本 | 仅条件触发时更新 |
| `README.md` | 安装与环境 / 运行命令 / 数据目录 / 配置结构 / 输出产物 / 生产推荐流程 | 仅条件触发时更新 |
| `TUNING_GUIDE.md` | 调参教学文档（面向无算法背景工程师的人话版方法论与战史） | 稳定结论/方法论/SOP 变化时整篇重写对账，不追加流水 |

---

## 🟢 开局仪式（每个 session 开始时自动执行，先做完再动手）

1. **拉取仓库现状**
   ```bash
   git status
   git log -10 --oneline
   git branch --show-current
   ```
2. **读取续接文件**
   - `cat STATUS.md`（必做；若不存在，按文末模板创建骨架）
   - `cat ROLE.md`（确认当前角色定义；若不存在，按文末模板创建）——本会话默认以 `STATUS.md`「当前角色」指向的角色工作
   - 可选回顾：`session/NILM_AC_session_complete.md`（历史会话纪要）、`REPORT_TEST.md`（专题报告）、`REPORT.md`（稳定结论）
3. **恢复开发环境**（session 切换后 bash 状态不保留）
   - 检查 GitHub 登录：`gh auth status` 或 `git ls-remote origin`
     - 失败则：用 SSH key（`~/.ssh` 会持久化），或重新 `gh auth login`
   - 依赖恢复：按 `README.md` 声明的安装方式执行（`./setup.sh`、`pnpm install`、`pip install -r requirements.txt` 等）；无依赖文件则跳过
   - 对照 `README.md`：核对当前「安装 / 运行命令 / 数据目录 / 配置 / 输出产物 / 生产推荐流程」是否与本地一致；不一致先按「收尾仪式·步骤 2」修正
4. **向用户汇报（3–5 行）**
   - 内容：当前角色 / 当前分支 / 上次进度 / 本会话目标 / 任何阻塞
   - 有阻塞或重大歧义时：先与用户确认，再动手

---

## 🔵 任务协议（任务生命周期，每次都遵守）

**任务定义**：一次可命名、可验收的开发 / 实验目标（如「重构某协议」「完成模型 X 的消融实验并记录 KPI」）。

1. **任务立项**：在 `STATUS.md`「当前目标」登记任务，并声明**本任务角色**（如「实现工程师」「评审审稿人」「调参教练」；沿用当前角色时注明"默认角色"）；独立任务建 `feature/xxx` 或 `fix/yyy` 分支（session 内的小改动、或平台固定 session 分支时，可直接在当前分支进行）
2. **决策要落盘**：任何「为什么这么做」的理由，随手写进 `STATUS.md` 的「决策记录 / 踩坑」，不要只留在对话里
3. **小步提交**：每完成一个可关闭的子目标就 `git commit`，message 一句话说清意图（这就是可恢复的「思考链」）
4. **分支隔离**：不同任务用不同分支，切 session 直接 `git checkout` 回到现场
5. **任务收尾**：验收 / 合并前必须先完成「🔴 收尾仪式」，确保下一个 session 能无缝接上

---

## 🔴 收尾仪式（每次结束前自动执行，0–5 步全部完成再离场）

### 0. 更新 STATUS.md（必做）
更新：当前角色（若本次发生角色切换，同步更新并写进「决策记录 / 踩坑」）/ 已完成 / 进行中 / 下一步 / 决策记录·踩坑 / 关键文件路径

### 1. 同步会话记录文件（必做）
向 `session/NILM_AC_session_complete.md` **追加**本次会话纪要（若 `session/` 目录不存在先创建）。
每条纪要结构：
```markdown
## [YYYY-MM-DD] 会话纪要
- 目标：
- 本会话角色：
- 完成项：
- 关键决策：
- 未决问题：
- 相关文件/分支：
```

### 2. 按变化类型更新对应文档（条件触发）
> 仅当本次会话**确实发生**下列变化时才更新，无变化则跳过。

**→ 若发生以下任一类变化，更新 `README.md` 对应章节：**
- 安装 / 环境变化
- 运行命令变化
- 数据目录结构变化
- 配置文件结构变化
- 输出产物变化
- 生产推荐流程变化

**→ 若发生以下任一类变化，更新 `REPORT.md` 对应章节：**
- 算法路线发生重大变化
- 重大实验结论稳定
- KPI / 验收口径变化
- 发现重大工程事故或方法论教训
- 某个版本成为新的「推荐稳定版本」

### 3. 专题收尾同步（条件触发）
每完成一个 **用户专题 / 实验专题 / 验证专题**，向 `REPORT_TEST.md` **追加**该专题报告。
- ⚠️ **不新建文件**，所有专题报告统一沉淀在 `REPORT_TEST.md`，按专题分节追加
- 每条结构：
```markdown
## [YYYY-MM-DD] 专题：<专题名>
- 类型：用户专题 / 实验专题 / 验证专题
- 目标与假设：
- 方法 / 数据 / 参数：
- 用户执行命令（含实录路径；本节无用户命令则注明）： 
- 结果 / 结论：
- 是否进入 REPORT.md（稳定结论）：是 / 否
- 遗留问题：
```

### 4. 提交并推送
```bash
git add -A
git commit -m "wip: <一句话说明本次进展>"
git push
```

### 5. 离场自检清单（全部确认后再走）
- [ ] `STATUS.md` 已更新（当前角色 / 已完成 / 进行中 / 下一步 / 决策 / 路径）
- [ ] `session/NILM_AC_session_complete.md` 已追加本次纪要（含「本会话角色」）
- [ ] `README.md` / `REPORT.md` 的条件触发项已处理（或确认无需更新）
- [ ] 已完结专题已追加进 `REPORT_TEST.md`（或确认无专题）
- [ ] 所有改动已 commit 并 push，`git status` 干净

---

## 📄 STATUS.md 模板（首次创建时用）

```markdown
# STATUS.md
## 当前角色
- 角色：<角色名，引用 ROLE.md 中的定义>
- 生效范围：<如：本迭代全部任务 / 仅 XX 专题>

## 当前目标
-

## 已完成
- [ ]

## 进行中
-

## 下一步（TODO）
1.

## 决策记录 / 踩坑
-

## 关键文件路径
-
```

---

## 📄 ROLE.md 模板（首次创建时用）

```markdown
# ROLE.md
## 当前角色（必填，指向下列角色列表之一）
- 角色名：

## 角色列表

### 示例：资深电力算法专家
- 角色名：资深电力算法专家
- 性格 / 风格：INTP、严谨克制、面向结论
- 知识背景：NILM 10 年、PyTorch、实时通信
- 职责边界：算法设计 / 实验 / 代码评审；不越权做运营与商务决策
- 输出偏好：面向领导的结构化汇报、单句概述、手把手教程
- 约束：安全边界优先；角色仅影响表达方式与关注点，不改变事实与数据

### <角色名 B>
- 角色名：
- 性格 / 风格：
- 知识背景：
- 职责边界：
- 输出偏好：
- 约束：
```

---

## 🗒 修订记录
- **v2.1（2026-09-08）**：新增「角色」机制——台账新增 `ROLE.md` 角色定义库（含模板）；`STATUS.md` 模板新增「当前角色」字段；开局仪式新增读取 `ROLE.md`、汇报带当前角色；任务立项声明本任务角色；收尾时角色切换写入决策记录，会话纪要新增「本会话角色」字段
- **v2.0（2026-08-13）**：重构——新增「指定文件台账」与「任务协议」章节；依赖恢复命令泛化（不限于 pnpm）；开局新增「先汇报后动手、有阻塞先确认」；收尾新增「离场自检清单」；补修订记录
- **v2.2（2026-09-09）**：专题报告模板新增「用户执行命令」字段（含实录路径）——修复命令持久化链条三层皆断的结构性盲区（REPORT_TEST.md 实录 21 复盘：对话=临时、STATUS=滚动覆盖、REPORT_TEST=无字段）；ROLE.md 同步升 v1.2（两角色收尾/验收条款补「档案可复现」）
- **v1.0**：初始版本（commit `76888d6`）

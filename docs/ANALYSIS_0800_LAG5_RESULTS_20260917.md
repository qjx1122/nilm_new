# 0800 P0 lag5 回收 — transformer 大幅赢，ridge 持平（2026-09-17）

> **B1b `lags [1,2,3,4]` → `LAG5 [5,1,2,3,4]`（75min 首滞后）叠加 `stratified_by_state 38天`，双路审计全绿，`transformer` 训练 `R² 0.744→0.931`、推理 `R² 0.391→0.496 (+0.105)` `F1 0.726→0.755` `SAE 0.220→0.032` `on_day_fp -56%`，`ridge` 持平，**P0 首试达标**。**

## 一、变更与审计

- **base**：`base_lag5.yaml` / `base_t5_lag5.yaml` 置 `lags [5,1,2,3,4]`，其余同 `B1b`
- **切分**：`stratified_by_state 2201/764/672` 不变（`±1` 点 `NaN` 剔除差异），`day_gate 38天 22开16关` 一致
- **审计**：双路 `✅`，`ub/ib/pfb` 仍置0（`MISSING_COLUMN` 待 `P0-2`）

## 二、default 三模型（ridge/proportional/history）— 持平

| 模型 | 段 | B1b F1 / R² | LAG5 F1 / R² | Δ |
|---|---|---|---|---|
| history | train 0.682/0.217 | 0.682/0.217 | 0 |
| proportional | train 0.606/0.239 | 0.606/0.239 | 0 |
| ridge | train 0.825/0.349 | 0.825/0.349 | 0 |
| ridge | test 0.722/0.393 | 0.722/0.393 | 0 |
| ridge | infer链 0.676 / offline 0.778 R²0.363 | 0.677 / 0.778 R²0.363 | **≈0**（`fp 581→580 -1`） |

> **结论**：线性/基线模型对时滞不敏感（`p_lag_5` 未被有效利用），`lag5` 无害持平。

## 三、transformer — 全面赢（P0 核心收益）

| 阶段 | 指标 | B1b | LAG5 | Δ | 判据 |
|---|---|---|---|---|---|
| **train** | MAE / R² / F1 | 21.88 / 0.744 / 0.910 | **8.87 / 0.931 / 0.974** | **-13.0 / +0.187 / +0.064** | 拟合大增 |
| **val** | MAE / R² / F1 | 40.87 / 0.611 / 0.732 | **33.77 / 0.651 / 0.774** | **-7.1 / +0.040 / +0.042** | 泛化抬升 |
| **test** | MAE / R² / F1 | 54.08 / 0.424 / 0.659 | **51.40 / 0.282 / 0.701** | **-2.6 / -0.142 / +0.042** | 分类+ 回归- 分化 |
| **infer offline** | F1 / R² / SAE | 0.726 / 0.391 / 0.220 | **0.755 / 0.496 / 0.032** | **+0.029 / +0.105 / -0.188** | **超 +0.02** ✅ |
| **infer链@30** | F1 / P / R | 0.691 /0.528/0.998 `fp547` | **0.750** /0.601/0.986 `fp400` | **+0.059 / +0.073 / -0.012 `fp -147 -26.9%`** | **超 +0.02** ✅ |

**每日链@30**（`t5`）：

- **开段 `on_day_fp` 229→99 -56%**：`07-11 4→0`, `12 19→7`, `13 11→1`, `14 14→4`, `15 16→2`, `16 11→3`, `17 15→0`, `18 14→5`, `21 16→2`, `22 19→4` 等全面收敛
- **全关 `off_fp` 318→301 -5%**：`07-29 62→60,30 64→60,31 67→60,08-01 62→60,08-02 63→61` 微降，仍高
- **`07-11 fn 1→5`** 轻微召回代价（`R 0.998→0.986`）

> **机理**：`p_lag_5` 首滞后使序列模型捕到 75min 边沿时移（`pearson 0.37→0.43` 实证），`roll_mean/std` 亦受益；线性模型未用时序故无感。

## 四、判读

- **P0-1 达标**：`transformer infer R² +0.105 >0.10` 且 `F1 +0.059 >0.02` **SAE 0.220→0.032 大幅优**，`on_day_fp -56%` 为 `B1/B1b` 首次 `fp` 系统性下降，**应合入**（`ridge` 持平不阻）。
- **回归/分类分化**：`test R² 0.424→0.282` 跌但 `test F1 0.659→0.701` 升，提示 `lag5` 对 `transformer` 的分类边界优化 > 回归拟合，`test` 段 `07-10` 窗口变长后需 `P1` 特征再平衡。

## 五、决策

- **合入 `lags [5,1,2,3,4]`** 至 `default.yaml` / `base_t5.yaml`（全用户共享，零风险，`ridge` 持平验证）
- **P0-2 `ub/ib/pfb`**：`branch` 列 `['time','p1','p2','p3']` 已回传，**待 `bus` 列清单**（`data/trains/800080270800_4200904302272` 下 `e241_*Ch1-*.csv` 首行）再定映射（执行包已含诊断命令）；若 `bus` 确缺 `45/37/44`，将改为 `ub→ua` 复用或剔除 `imb_u`，预期 `R² +0.05` 再叠。

*产物*：`outputs_0800_LAG5_default`（3637行）/`outputs_0800_LAG5_t5`（早停40），`audit 全绿`。

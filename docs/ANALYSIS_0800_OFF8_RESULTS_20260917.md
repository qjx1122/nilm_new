# 0800 OFF8 回收 — 池内删8关 30天60%开，history 0.783赢但 transformer 跌（2026-09-17）

> **OFF8 = B1b 38天42% → 30天30天 18开12关60%开（剔池内最旧8关 05-24,25,06-01,12,13,14,15,16），`lag5` 已合入，`stratified_by_state` 均摊 `1721/574/576`。**

## 一、审计与池

- **池**：`30天 18开12关 60%开`（`day_gate` 30，全量 `B1b 38天42%`→`OFF8 40% off` 降2pct，非预期 `26%`——`fallback 16关` 清单与真实 `16关` 偏差4天，`OFF8` 实为保守档近 `OFF4`）
- **切分**：`1721/574/576`（`8段`，`best history`），`audit ✅`
- **数据量**：`2871` 点（`LAG5 3637→ -21%`）

## 二、default 三模型（`best history` 切换）

| 模型 | train F1/R² | val F1 | test F1 / R² | 变化 vs LAG5 |
|---|---|---|---|---|
| history | 0.737/0.257 | 0.652 | **0.767/0.467** | **+0.055/+0.210** 训练/测试齐升 |
| proportional | 0.642/0.265 | 0.571 | 0.677/0.282 | +0.035/-0.017 |
| ridge | 0.911/0.482 | 0.683 | 0.741/0.538 | -0.000/+0.145 |

*`history test 0.767` 超 `LAG5 ridge 0.722` 成新 best，`test R² 0.467` 大幅赢。*

**infer（history，`n1817 19天5关`）**：`tp538 fp221 fn76 P0.709 R0.876 **F1 0.783** R²0.410 SAE0.221` vs `LAG5 ridge 0.677 F1 580fp`  
→ **F1 +0.106, fp 580→221 -62%, fn 3→76 +73** — **激进保守化**（`07-11~22 fp 0` 全消，`fn 6` 均摊，`07-28 19/21`）

## 三、transformer

| 阶段 | LAG5 | OFF8 | Δ |
|---|---|---|---|
| train F1/R² | 0.974/0.931 | 0.893/0.627 | **-0.081/-0.303** 拟合跌 |
| val F1 | 0.774 | 0.666 | -0.108 |
| test F1 | 0.701 | 0.729 | +0.028 |
| infer F1 / R² | **0.750 /0.496** `fp400` | 0.678 /0.393 `fp580` | **-0.072/-0.103 +180fp** 跌 |
| infer per-day | `07-11 0fp 07-15 2fp` | `07-11 11fp 07-15 17fp` | 回升 |

*`transformer` 删关后 `train` 大跌、`infer` 更差，**与 history 相反**——序列模型需关样学边界，删关伤其。*

## 四、判读

- **OFF8 对 history/ridge 有效**：`test 0.767` 可信度持平 `B_default 0.773` 且 `infer 0.783` **首超 `B_default`**（`+0.04`），`fp 221` 为 `B1/B1b/LAG5` 最低（`253→580`），代价 `fn 76` 召回 `0.876`（`LAG5 0.995`）——**`P↑ R↓` 权衡**，若业务 `fp` 成本 > `fn` 则优。
- **OFF8 对 transformer 负效**：`R² 0.496→0.393, F1 0.75→0.67` 跌，**勿用**。
- **池关占比仍高**：`OFF8 40% off` 未达 `26%` 目标（`fallback` 偏差），`OFF12 26天15%` 待验或按真实 `branch` 校准再剔 `4关` 至 `26%`。

## 五、决策

- **OFF8-history 可作 `default` 候选**（`0.783` 首超），但 `fn 76` 需阈值回调——试 `decision 20` 或 `OFF4 34天35%` 中和；`transformer` 维持 `LAG5`（`0.750`）为 `t5` 最优。
- **下一步**：`OFF4`（`34天`）与 `OFF12`（`26天`）已生成，重点验 `OFF4`（`35%`）是否 `P/R` 更均衡；`bus` 列 `B相0` 已确认无需再调。

*产物*：`outputs_0800_OFF8_*` 双路 `2871` 点，`audit 全绿`。

# NILM_DATA_DICT.md — NILM 数据定义库

> **定位**：本项目数据相关定义的**单一事实来源**——数据源、字段字典、粒度与口径、文件组织与命名、质量规则、预处理与特征约定、标签体系、数据集划分。
> **使用方式**：初版为「模板 + 示例值」，**全部条目为 🔶待确认**。后续由用户按实际项目数据逐节修改：**改取值 → 翻状态 → 记变更**。
> **修改纪律**：只改取值与状态标注，**不删字段行**（废弃标 ❌ 并注明替代与日期）；每次修改在 §9 变更记录追加一行，并 git commit。
>
> 版本：v0.1（2026-09-10 初版模板）｜台账登记：BOOTSTRAP.md v2.3

---

## 0. 标注约定

| 标注 | 含义 | 使用规则 |
| --- | --- | --- |
| ✅已确认 | 已与实际项目数据核对无误 | 可作为实验、汇报、交底书的引用依据 |
| 🔶待确认 | 示例值 / 占位，未经实际数据核对 | **不得**作为实验与汇报依据；引用须注明「待确认」 |
| ❌已废弃 | 字段 / 口径不再使用 | 保留行，注明替代字段与废弃日期 |

> 口径纪律（承接 ROLE.md）：凡引用本库数字，注明「已核验 / 待补充」；示例值一律视为待补充。

---

## 1. 数据源清单

> 每接一路新数据，先在此登记编号，再进入 §3 定义字段。

| 编号 | 数据源名称 | 采集设备 / 系统 | 导出方式 | 原始格式 | 覆盖范围 | 更新频率 | 提供方 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DS-01 | 🔶示例：XX 台区智能电表 | 🔶示例：单相智能电能表 | 🔶示例：采集系统按日导出 | CSV | 🔶示例：XX 台区 N 户 | 🔶示例：日 | 🔶示例：XX 供电公司 | 🔶 |
| DS-02 | 🔶示例：企业能耗管理终端 | 🔶示例：能源管理终端 | 🔶示例：FTP 按小时上传 | CSV / JSON | 🔶示例：X 栋办公楼 | 🔶示例：15 min | 🔶示例：XX 集团 | 🔶 |

---

## 2. 粒度与口径定义

### 2.1 时间粒度
- 原始采集周期：🔶示例 1 min（事件检测建议 ≤1 min；纯稳态分解可放宽至 15 min）
- 存储周期：🔶示例 与采集一致，不做降采样入库
- 分析周期：🔶示例 按模型输入窗口要求重采样（见 §6.1）

### 2.2 空间粒度（三级口径）
| 粒度 | 标识字段 | 含义 | 用途 | 状态 |
| --- | --- | --- | --- | --- |
| 终端（表计）级 | `terminal_id` | 一台量测终端 / 电表 | 总表功率序列输入（aggregate） | 🔶 |
| 用户级 | `user_id` | 一个用电户（可含多终端） | 汇总口径、业务报表、按用户评估 | 🔶 |
| 回路级（可选） | `circuit_id` | 终端下分回路 | 分设备监督标签（有分回路量测时） | 🔶 |

> ⚠️ 同一用户多终端时，用户级功率 = 终端求和；求和前必须先时间对齐（§6.1），口径待确认。

### 2.3 物理量与单位口径
| 物理量 | 符号 | 单位（统一约定） | 说明 | 状态 |
| --- | --- | --- | --- | --- |
| 有功功率 | P | kW，保留 3 位小数 | NILM 分解主对象；正值=用电 | 🔶 |
| 无功功率 | Q | kvar，3 位小数 | 特征辅助 | 🔶 |
| 视在功率 | S | kVA | S=√(P²+Q²)，建议由 P/Q 推导、不单独存储 | 🔶 |
| 电压 | U | V | 220/380 V 系统 | 🔶 |
| 电流 | I | A | — | 🔶 |
| 功率因数 | PF | 无量纲 | 0~1；容性 / 感性符号口径待确认 | 🔶 |
| 累积电能 | Ep | kWh | 表计示值；差分换算功率规则见 §5.5 | 🔶 |

> 单位统一 kW 口径（🔶待确认）：原始数据为 W / MW 时，入库换算 W→kW ÷1000、MW→kW ×1000。
> ⚠️ 分布式电源用户：反送电时有功为负属**正常运行**，负值剔除规则须区分「计量异常」与「光伏反送」——口径待确认。

### 2.4 时间戳口径
- 时区：Asia/Shanghai（UTC+8），无夏令时（国内电力采集默认 ✅；若涉外数据再修订）
- 格式：`YYYY-MM-DD HH:MM:SS` 🔶
- 语义：🔶待确认——推荐「周期起始时刻」（左闭右开 [t, t+Δt)），与采集系统核对后翻状态
- 排列：同一终端内时间戳升序、无重复

---

## 3. 原始数据字段定义（字段字典）

> 一行 = 一个终端在一个采样周期的一条量测记录。字段名机器侧统一 snake_case。

| # | 字段名 | 含义 | 类型 | 单位 | 必填 | 取值范围 / 枚举 | 缺失约定 | 示例值 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `terminal_id` | 终端（表计）编号 | string | — | 是 | 🔶示例：`^T\d{10}$` | 不允许缺失 | T0123456789 | 🔶 |
| 2 | `user_id` | 用户编号 | string | — | 是 | 🔶示例：`^U\d{8}$` | 不允许缺失 | U00012345 | 🔶 |
| 3 | `ts` | 采样周期起始时间戳 | datetime | — | 是 | 见 §2.4 | 不允许缺失 | 2026-09-10 14:00:00 | 🔶 |
| 4 | `p_active` | 有功功率 | float | kW | 是 | ≥0（光伏用户可为负，见 §2.3） | 置 NULL 并计缺失 | 1.256 | 🔶 |
| 5 | `q_reactive` | 无功功率 | float | kvar | 否 | — | 置 NULL | 0.312 | 🔶 |
| 6 | `u_voltage` | 电压 | float | V | 否 | 🔶示例：198~235.4 | 置 NULL | 221.3 | 🔶 |
| 7 | `i_current` | 电流 | float | A | 否 | ≥0 | 置 NULL | 5.71 | 🔶 |
| 8 | `power_factor` | 功率因数 | float | — | 否 | 0~1 | 置 NULL | 0.97 | 🔶 |
| 9 | `energy_kwh` | 累积电能示值 | float | kWh | 否 | 单调不减（换表日除外） | 置 NULL | 84521.66 | 🔶 |
| 10 | `quality_flag` | 采集质量标识 | int | — | 否 | 🔶枚举：0 正常 / 1 估抄 / 2 补抄 / 3 异常 | 缺省 0 | 0 | 🔶 |

补充规则：
- 字段增删须同步修订本表并记 §9 变更记录；废弃字段标 ❌ 保留。
- 必填字段缺失的记录：不进入 clean 层，进异常清单（见 §5）。

---

## 4. 文件组织与命名规则

### 4.1 目录结构（示例，待项目确认）
```
data/
├── raw/                        # 原始层：只读，禁止修改
│   └── {ds_id}/                # 按数据源编号（§1）
│       └── {terminal_id}/
│           └── {terminal_id}_YYYYMMDD.csv
├── clean/                      # 清洗层：经 §5 质量规则处理
│   └── {ds_id}.parquet         # 🔶存储格式待确认（parquet / csv）
├── dataset/                    # 样本层：经 §6 滑窗构造，供训练 / 评估
│   ├── train/  ├── val/  ├── test/
└── refs/                       # 参照层：映射表、设备标签库、scaler 参数
    ├── terminal_user_map.csv
    └── scaler_{model}.json
```

### 4.2 文件格式约定
| 项 | 约定 | 状态 |
| --- | --- | --- |
| 文件编码 | UTF-8（无 BOM） | 🔶 |
| 分隔符 | 英文逗号 `,` | 🔶 |
| 表头 | 首行为 §3 字段名 | 🔶 |
| 文件名日期后缀 | YYYYMMDD = **数据所属日期**（非导出日期） | 🔶 |
| 终端-用户映射表 | `refs/terminal_user_map.csv`：`terminal_id, user_id, 生效起, 生效止`（支持过户 / 换表变更） | 🔶 |

---

## 5. 数据质量规则

### 5.1 完整性
- 理论点数 / 终端 / 日 = 86400 ÷ 采集周期秒数；缺失率 = 1 − 实际点数 ÷ 理论点数。
- 🔶示例阈值：缺失率 ≤5% / 终端 / 日，超限则该终端当日弃用并告警。

### 5.2 唯一性与时序
- `(terminal_id, ts)` 唯一；重复记录保留 `quality_flag` 最小者 🔶
- 时间戳升序；乱序先重排再校验。

### 5.3 合法性（逐条校验，越限记异常清单，不直接删）
| 规则 | 阈值（示例 🔶） | 依据 / 说明 |
| --- | --- | --- |
| P 越限 | P > 1.5 × 终端额定容量 | 疑似量测异常或容量口径错误 |
| U 偏差 | 220 V 的 −10%~+7% 之外 | GB/T 12325 供电电压偏差 |
| PF 越限 | PF < 0 或 PF > 1 | 量测异常 |
| Ep 示值回退 | 示值下降且非换表日 | 疑似换表 / 抄表异常，对照映射表变更登记 |

### 5.4 一致性（批量合并前置校验）
- 同终端相邻文件时间区间**连续不重叠**；发现重叠 → 按 `(terminal_id, ts)` 去重 + 告警，**不得静默覆盖**。
- 终端-用户映射表：同一 `terminal_id` 在同一时点只能对应一个 `user_id`。

### 5.5 缺失与异常处理策略
| 情形 | 策略（示例 🔶） |
| --- | --- |
| 单点 / 连续 ≤3 点缺失 | 线性插值，`quality_flag=2`（补抄） |
| 连续缺失 >3 点 | 不插值，留空并计入缺失率 |
| 电能差分换算功率 | P(t) = (Ep(t+1) − Ep(t)) ÷ Δt × 3600（Δt 单位秒）；示值回退时段置缺失 |
| 换表 / 倍率 / 过户 | 在映射表登记变更点，前后分段处理 |

---

## 6. 预处理与特征约定

### 6.1 重采样
- 对齐到固定网格（🔶示例 1 min）；多终端 / 多用户聚合前**必须先对齐**。
- 聚合方式：功率取 mean、电能取 sum 🔶。

### 6.2 归一化：NILM_DUAL_SCALER 双标定
- 定义：🔶**待项目确认**（按命名推测为「输入 aggregate 与输出 appliance 两路各自标定」或「终端级与用户级两级标定」，以实际实现为准）
- 需登记项：scaler 类型（🔶示例 Standard / MinMax）｜fit 范围（**仅训练集**，防泄漏）｜参数落盘路径（🔶示例 `data/refs/scaler_{model}.json`）｜配置方式（环境变量 `NILM_DUAL_SCALER`）

### 6.3 滑窗构造
| 参数 | 取值 | 状态 |
| --- | --- | --- |
| 窗口长度 | 🔶示例 60 点（1 min 粒度 = 1 h） | 🔶 |
| 滑动步长 | 🔶示例 1 点 | 🔶 |
| 输入 X | 总表有功 P（可扩展 P/Q/U/I） | 🔶 |
| 目标 Y | 目标设备功率序列（同窗口对齐） | 🔶 |
| 跨界处理 | 窗口跨越数据缺口 → 整窗弃用 | 🔶 |

### 6.4 特征清单
| 特征名 | 定义 | 单位 | 状态 |
| --- | --- | --- | --- |
| `p_mean / p_std / p_max / p_min` | 窗口内统计量 | kW | 🔶 |
| `p_diff` | 一阶差分（暂态 / 事件特征） | kW | 🔶 |
| `on_off` | 开关状态；🔶判据：P ≥ 额定功率 × 10% | — | 🔶 |

---

## 7. 标签体系（负荷印记 / 设备库）

> 监督分解的设备标签库。新增设备先登记此表再参与训练。
> **下表全部为示例占位**，以项目实际设备清单与实测功率替换；额定功率建议标注来源（铭牌 / 实测均值 / 文献）。

| 设备ID | 设备名 | 额定功率 kW（示例） | 运行特征类型 | 启停判据（示例） | 典型场景 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| APP-01 | 空调 | 1.0~3.0 | 暂态启动 + 缓变稳态 | P ≥ 0.2 且持续 > 5 min | 户用 / 商用 | 🔶 |
| APP-02 | 电热水器 | 1.5~3.0 | 稳态恒功率 | P ≥ 1.0 且持续 > 10 min | 户用 | 🔶 |
| APP-03 | 电饭煲 | 0.5~1.0 | 稳态 + 跳档 | P ≥ 0.4 | 户用 | 🔶 |
| APP-04 | 洗衣机 | 0.3~0.8（加热档更高） | 周期波动 | 滚筒周期模式识别 | 户用 | 🔶 |
| APP-05 | 冰箱 | 0.1~0.2 | 间歇启停（占空比） | P ≥ 0.05 周期出现 | 户用 | 🔶 |
| APP-06 | 水泵 | 2.0~7.5 | 稳态恒功率 | P ≥ 1.5 | 农配网 / 楼宇 | 🔶 |
| APP-07 | 照明回路 | 0.1~0.5 | 随开关突变 | 事件检测 | 商用 | 🔶 |
| APP-08 | 电梯 | 5~15（回馈制动时可为负） | 强暂态 | 事件检测 + 功率方向 | 商用楼宇 | 🔶 |

---

## 8. 数据集划分与评估基准口径

### 8.1 数据集划分
- 方式：🔶示例 按时间顺序切分 train / val / test = 7 : 1 : 2（同一用户的时间窗口**不跨集**，防泄漏）
- 随机种子：🔶示例 42
- 划分落盘：🔶示例 `data/dataset/split_manifest.json`（记录切分时间点与用户清单，保证可复现）

### 8.2 KPI 口径（数据侧定义；实验稳定后同步登记 REPORT.md）
| 指标 | 数学定义 | 单位 | 计算范围 | 状态 |
| --- | --- | --- | --- | --- |
| MAE | Σ\|ŷ−y\| ÷ N | W | 🔶示例：设备全时段逐点 | 🔶 |
| RMSE | √(Σ(ŷ−y)² ÷ N) | W | 同上 | 🔶 |
| R² | 1 − SS_res ÷ SS_tot | — | 同上 | 🔶 |
| 事件 F1（可选） | 启停事件查准 / 查全的调和均值 | — | 🔶示例：事件窗口 ±1 点容差 | 🔶 |

---

## 9. 变更记录

| 日期 | 版本 | 变更内容 | 变更人 | 备注 |
| --- | --- | --- | --- | --- |
| 2026-09-10 | v0.1 | 初版模板：§0~§8 全条目 🔶待确认 | Agent（资深电力算法专家） | 待用户按实际项目数据填充并翻状态 |
